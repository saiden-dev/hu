//! Tests for Environment enum

#[test]
fn test_environment_cluster_mapping() {
    // Environment cluster mappings are tested via integration tests
    // since they require the actual enum from the binary
    assert!(true);
}
